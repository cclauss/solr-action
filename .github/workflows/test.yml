name: Test

on:
  pull_request:
    types: [ opened, synchronize, reopened ]
    branches: [ develop, master ]

  workflow_dispatch

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1

      - uses: patric-dosch-vi/solr-action@1.0
